# Instructions to run the k-hop induction heads
1. Install the dependencies by `cd model/` and then running `pip install -e .`
2. Generate the samples by running `generate_samples.py`, specifying the number of runs, their output directory, etc.
3. Execute the experiment by running `indheads.py` with the desired specifications